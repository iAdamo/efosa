const GenerateHeaders = (props) => {
    const {} = props;

    return (
        <>
            <div>
                <button>Generate headers</button>
            </div>
            Generate
        </>
    );
};

export default GenerateHeaders;
